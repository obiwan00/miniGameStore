{
  "name": "mini-game-store",
  "version": "0.0.0",
  "scripts": {
    "start": "npm --prefix server run start",
    "heroku-postbuild": "npm --prefix client run build-aot",
    "dev": "concurrently --names \"A,N\" --prefix-colors \"red,green\" \"npm --prefix client run start\" \"npm --prefix server run dev\""
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^6.2.1"
  }
}
